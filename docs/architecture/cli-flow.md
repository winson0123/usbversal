# CLI Flow

**Status:** retired.

The argparse CLI under `app/cli/` was a test harness for the service layer.
It has been removed. The product is the TUI (`usbversal` / `python -m app.tui`).

Historical command list and `--mount` examples live in completed-task records
and older planning notes. Do not add them back.
